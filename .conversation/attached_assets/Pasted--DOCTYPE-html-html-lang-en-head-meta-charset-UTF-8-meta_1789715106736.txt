<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>RAISC — Robotics, AI & Intelligent Systems Club, GKCIET</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #F5F6F3;
    --surface: #FFFFFF;
    --ink: #14201A;
    --ink-muted: #55645C;
    --accent: #1F6E4A;
    --accent-dark: #164F35;
    --accent-soft: #E4EFE8;
    --line: #DCE3DE;
    --radius: 6px;
    --max: 1120px;
  }

  *{ box-sizing: border-box; }
  html{ scroll-behavior: smooth; }
  body{
    margin: 0;
    background: var(--bg);
    color: var(--ink);
    font-family: 'Inter', sans-serif;
    line-height: 1.55;
    -webkit-font-smoothing: antialiased;
  }
  h1, h2, h3, .logo-text{
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 600;
    line-height: 1.15;
    margin: 0;
  }
  a{ color: inherit; }
  img, svg{ display: block; max-width: 100%; }
  ul{ margin: 0; padding: 0; list-style: none; }
  section{ padding: 88px 0; }
  .wrap{
    max-width: var(--max);
    margin: 0 auto;
    padding: 0 32px;
  }
  .eyebrow{
    color: var(--accent-dark);
    font-weight: 600;
    font-size: 0.95rem;
    margin: 0 0 12px;
  }
  .btn{
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 22px;
    border-radius: var(--radius);
    font-family: 'Inter', sans-serif;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    border: 1px solid transparent;
    cursor: pointer;
  }
  .btn-primary{ background: var(--accent); color: #fff; }
  .btn-primary:hover{ background: var(--accent-dark); }
  .btn-outline{ background: transparent; color: var(--ink); border-color: var(--line); }
  .btn-outline:hover{ border-color: var(--accent); color: var(--accent-dark); }

  /* ---- Nav ---- */
  header{
    position: sticky;
    top: 0;
    z-index: 20;
    background: rgba(245,246,243,0.92);
    backdrop-filter: blur(6px);
    border-bottom: 1px solid var(--line);
  }
  nav{
    max-width: var(--max);
    margin: 0 auto;
    padding: 18px 32px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .logo{
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: var(--ink);
  }
  .logo-text{ font-size: 1.15rem; letter-spacing: 0.01em; }
  .nav-links{
    display: flex;
    align-items: center;
    gap: 34px;
  }
  .nav-links a{
    text-decoration: none;
    color: var(--ink-muted);
    font-weight: 500;
    font-size: 0.95rem;
  }
  .nav-links a:hover{ color: var(--ink); }
  .nav-actions{ display: flex; align-items: center; gap: 18px; }
  .menu-toggle{
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }
  .menu-toggle span{
    display: block;
    width: 22px;
    height: 2px;
    background: var(--ink);
    margin: 5px 0;
  }

  /* ---- Hero ---- */
  .hero{ padding: 96px 0 72px; }
  .hero-grid{
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 56px;
    align-items: center;
  }
  .hero h1{
    font-size: clamp(2.1rem, 4vw, 3.1rem);
    max-width: 15ch;
  }
  .hero p.lead{
    margin: 22px 0 32px;
    max-width: 46ch;
    color: var(--ink-muted);
    font-size: 1.08rem;
  }
  .hero-actions{ display: flex; gap: 14px; flex-wrap: wrap; }
  .hero-art{
    position: relative;
    aspect-ratio: 1 / 1;
    max-width: 380px;
    margin-left: auto;
  }

  .stats{
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 64px;
    padding-top: 40px;
    border-top: 1px solid var(--line);
  }
  .stat-value{ font-family: 'Space Grotesk', sans-serif; font-size: 1.4rem; font-weight: 600; }
  .stat-label{ color: var(--ink-muted); font-size: 0.9rem; margin-top: 4px; }

  /* ---- About ---- */
  .about{ background: var(--surface); border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); }
  .about-grid{
    display: grid;
    grid-template-columns: 0.85fr 1.15fr;
    gap: 64px;
    align-items: start;
  }
  .about h2{ font-size: 1.9rem; max-width: 12ch; }
  .about-body p{ color: var(--ink-muted); font-size: 1.03rem; max-width: 62ch; }
  .about-body p + p{ margin-top: 16px; }

  /* ---- Focus areas ---- */
  .focus-grid{
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--line);
    border: 1px solid var(--line);
    margin-top: 48px;
  }
  .focus-card{
    background: var(--surface);
    padding: 32px 28px;
  }
  .focus-card .bar{
    width: 32px;
    height: 4px;
    background: var(--accent);
    margin-bottom: 22px;
  }
  .focus-card h3{ font-size: 1.15rem; margin-bottom: 10px; }
  .focus-card p{ color: var(--ink-muted); font-size: 0.97rem; }

  .section-head{
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 24px;
    flex-wrap: wrap;
  }
  .section-head h2{ font-size: 1.9rem; max-width: 16ch; }
  .section-head p{ color: var(--ink-muted); max-width: 40ch; margin: 0; }

  /* ---- Activities ---- */
  .activities-list{
    margin-top: 48px;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0;
    border-top: 1px solid var(--line);
  }
  .activity{
    display: flex;
    gap: 20px;
    padding: 28px 0;
    border-bottom: 1px solid var(--line);
  }
  .activity:nth-child(odd){ padding-right: 32px; border-right: 1px solid var(--line); }
  .activity:nth-child(even){ padding-left: 32px; }
  .activity-icon{
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--accent-soft);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .activity h3{ font-size: 1.05rem; margin-bottom: 6px; }
  .activity p{ color: var(--ink-muted); font-size: 0.95rem; margin: 0; }

  /* ---- Join / CTA ---- */
  .cta{
    background: var(--accent-dark);
    color: #fff;
    border-radius: 10px;
    padding: 56px 48px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 32px;
    flex-wrap: wrap;
  }
  .cta h2{ color: #fff; font-size: 1.7rem; max-width: 16ch; }
  .cta p{ color: rgba(255,255,255,0.78); margin: 10px 0 0; max-width: 40ch; }
  .cta .btn-outline{ border-color: rgba(255,255,255,0.4); color: #fff; }
  .cta .btn-outline:hover{ border-color: #fff; color: #fff; }
  .cta .btn-primary{ background: #fff; color: var(--accent-dark); }
  .cta .btn-primary:hover{ background: var(--accent-soft); }

  /* ---- Footer ---- */
  footer{ border-top: 1px solid var(--line); padding: 40px 0; }
  .footer-row{
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
  }
  .footer-links{ display: flex; gap: 26px; }
  .footer-links a{ text-decoration: none; color: var(--ink-muted); font-size: 0.92rem; }
  .footer-links a:hover{ color: var(--ink); }
  .footer-note{ color: var(--ink-muted); font-size: 0.88rem; }

  @media (max-width: 900px){
    .hero-grid{ grid-template-columns: 1fr; }
    .hero-art{ margin: 0 auto; max-width: 260px; order: -1; }
    .about-grid{ grid-template-columns: 1fr; gap: 28px; }
    .focus-grid{ grid-template-columns: 1fr; }
    .activities-list{ grid-template-columns: 1fr; }
    .activity:nth-child(odd){ padding-right: 0; border-right: none; }
    .activity:nth-child(even){ padding-left: 0; }
    .stats{ grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 700px){
    .nav-links{
      position: absolute;
      top: 100%; left: 0; right: 0;
      background: var(--surface);
      flex-direction: column;
      align-items: flex-start;
      padding: 20px 32px;
      border-bottom: 1px solid var(--line);
      display: none;
      gap: 18px;
    }
    .nav-links.open{ display: flex; }
    .menu-toggle{ display: block; }
    .cta{ flex-direction: column; align-items: flex-start; padding: 40px 28px; }
    section{ padding: 64px 0; }
  }

  @media (prefers-reduced-motion: reduce){
    html{ scroll-behavior: auto; }
  }
</style>
</head>
<body>

<header>
  <nav>
    <a href="#top" class="logo">
      <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
        <circle cx="14" cy="6" r="2.6" fill="#1F6E4A"/>
        <circle cx="5" cy="20" r="2.6" fill="#1F6E4A"/>
        <circle cx="23" cy="20" r="2.6" fill="#1F6E4A"/>
        <path d="M14 8.6V14M14 14L6.5 18.5M14 14L21.5 18.5" stroke="#1F6E4A" stroke-width="1.6"/>
      </svg>
      <span class="logo-text">RAISC</span>
    </a>
    <ul class="nav-links" id="navLinks">
      <li><a href="#about">About</a></li>
      <li><a href="#focus">Focus areas</a></li>
      <li><a href="#activities">Activities</a></li>
      <li><a href="#join">Join</a></li>
    </ul>
    <div class="nav-actions">
      <a class="btn btn-primary" href="https://www.linkedin.com/company/raiscgkciet" target="_blank" rel="noopener">Follow</a>
      <button class="menu-toggle" id="menuToggle" aria-label="Toggle menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </nav>
</header>

<main id="top">

  <section class="hero">
    <div class="wrap">
      <div class="hero-grid">
        <div>
          <p class="eyebrow">GKCIET Robotics Club</p>
          <h1>Robotics, AI & Intelligent Systems, built by students.</h1>
          <p class="lead">RAISC is a student-driven technical club at GKCIET, committed to technical excellence, innovation, research and collaborative learning across robotics and emerging intelligent technologies.</p>
          <div class="hero-actions">
            <a class="btn btn-primary" href="https://www.linkedin.com/company/raiscgkciet" target="_blank" rel="noopener">Follow on LinkedIn</a>
            <a class="btn btn-outline" href="https://www.gkciet.ac.in/facility/rc" target="_blank" rel="noopener">Visit college page</a>
          </div>
        </div>
        <div class="hero-art">
          <svg viewBox="0 0 360 360" width="100%" height="100%">
            <circle cx="180" cy="180" r="150" fill="none" stroke="#DCE3DE" stroke-width="1.5"/>
            <circle cx="180" cy="60" r="8" fill="#1F6E4A"/>
            <circle cx="80" cy="230" r="8" fill="#1F6E4A"/>
            <circle cx="280" cy="230" r="8" fill="#1F6E4A"/>
            <circle cx="180" cy="300" r="8" fill="#E4EFE8" stroke="#1F6E4A" stroke-width="2"/>
            <circle cx="180" cy="180" r="10" fill="#164F35"/>
            <path d="M180 68 L180 172" stroke="#1F6E4A" stroke-width="1.5"/>
            <path d="M88 224 L173 186" stroke="#1F6E4A" stroke-width="1.5"/>
            <path d="M272 224 L187 186" stroke="#1F6E4A" stroke-width="1.5"/>
            <path d="M180 190 L180 292" stroke="#1F6E4A" stroke-width="1.5"/>
          </svg>
        </div>
      </div>

      <div class="stats">
        <div>
          <div class="stat-value">2020</div>
          <div class="stat-label">Founded</div>
        </div>
        <div>
          <div class="stat-value">Student-run</div>
          <div class="stat-label">Governance</div>
        </div>
        <div>
          <div class="stat-value">Robotics · AI · Systems</div>
          <div class="stat-label">Core focus</div>
        </div>
      </div>
    </div>
  </section>

  <section class="about" id="about">
    <div class="wrap">
      <div class="about-grid">
        <h2>About the club</h2>
        <div class="about-body">
          <p>RAISC — the Robotics, Artificial Intelligence & Intelligent Systems Club — brings together students at GKCIET who want to build things and learn beyond the classroom. The club promotes technical excellence, innovation, research and collaborative learning in emerging technologies.</p>
          <p>Since 2020, RAISC has run as a nonprofit, student-driven community, giving members hands-on exposure to robotics hardware, applied AI and the systems that connect the two.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="focus" id="focus">
    <div class="wrap">
      <div class="section-head">
        <h2>What the club works on</h2>
        <p>Three connected areas, from hardware to intelligence to integration.</p>
      </div>
      <div class="focus-grid">
        <div class="focus-card">
          <div class="bar"></div>
          <h3>Robotics</h3>
          <p>Mechanical design, embedded systems and control — building the machines that sense and act in the physical world.</p>
        </div>
        <div class="focus-card">
          <div class="bar"></div>
          <h3>Artificial Intelligence</h3>
          <p>Applied machine learning and perception, from data to models that make sense of the world around a robot.</p>
        </div>
        <div class="focus-card">
          <div class="bar"></div>
          <h3>Intelligent Systems</h3>
          <p>Bringing sensing, decision-making and automation together into systems that work reliably end to end.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="activities" id="activities">
    <div class="wrap">
      <div class="section-head">
        <h2>How the club runs</h2>
        <p>A mix of learning, building and competing, driven by members.</p>
      </div>
      <div class="activities-list">
        <div class="activity">
          <div class="activity-icon">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M3 9h12M9 3v12" stroke="#1F6E4A" stroke-width="1.6"/></svg>
          </div>
          <div>
            <h3>Workshops & hands-on sessions</h3>
            <p>Sessions on electronics, embedded programming and ML fundamentals, run by members for members.</p>
          </div>
        </div>
        <div class="activity">
          <div class="activity-icon">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><rect x="3" y="3" width="12" height="12" rx="2" stroke="#1F6E4A" stroke-width="1.6"/></svg>
          </div>
          <div>
            <h3>Projects & prototyping</h3>
            <p>Small teams design, build and iterate on robots and intelligent-systems projects through the year.</p>
          </div>
        </div>
        <div class="activity">
          <div class="activity-icon">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M9 2 L15 6 V12 L9 16 L3 12 V6 Z" stroke="#1F6E4A" stroke-width="1.6"/></svg>
          </div>
          <div>
            <h3>Competitions & hackathons</h3>
            <p>Teams represent RAISC at robotics and AI competitions, applying what they've built under time pressure.</p>
          </div>
        </div>
        <div class="activity">
          <div class="activity-icon">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M3 4h12M3 9h12M3 14h8" stroke="#1F6E4A" stroke-width="1.6"/></svg>
          </div>
          <div>
            <h3>Research & reading groups</h3>
            <p>Regular discussion of papers and ideas in robotics and AI, keeping the club close to current research.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="join" id="join">
    <div class="wrap">
      <div class="cta">
        <div>
          <h2>Want to get involved?</h2>
          <p>Follow RAISC for updates on workshops, projects and events, or visit the club's page on the GKCIET website.</p>
        </div>
        <div class="hero-actions">
          <a class="btn btn-primary" href="https://www.linkedin.com/company/raiscgkciet" target="_blank" rel="noopener">Follow on LinkedIn</a>
          <a class="btn btn-outline" href="https://www.gkciet.ac.in/facility/rc" target="_blank" rel="noopener">Club page</a>
        </div>
      </div>
    </div>
  </section>

</main>

<footer>
  <div class="wrap footer-row">
    <span class="footer-note">RAISC — Robotics, AI & Intelligent Systems Club, GKCIET</span>
    <div class="footer-links">
      <a href="https://www.gkciet.ac.in/facility/rc" target="_blank" rel="noopener">College page</a>
      <a href="https://www.linkedin.com/company/raiscgkciet" target="_blank" rel="noopener">LinkedIn</a>
    </div>
    <span class="footer-note">© 2026 RAISC</span>
  </div>
</footer>

<script>
  const toggle = document.getElementById('menuToggle');
  const links = document.getElementById('navLinks');
  toggle.addEventListener('click', () => {
    const open = links.classList.toggle('open');
    toggle.setAttribute('aria-expanded', open);
  });
  links.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
    links.classList.remove('open');
    toggle.setAttribute('aria-expanded', 'false');
  }));
</script>

</body>
</html>